# Agent Rules

- After successfully making any modifications or updates to the codebase (such as adding features, fixing bugs, or refactoring), always commit the changes to Git with a descriptive commit message and push them to the GitHub repository automatically.
